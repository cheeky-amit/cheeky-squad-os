---
mode: <one-time | multi-use | evergreen>
created: <ISO-8601 datetime>
target: <ISO-8601 deadline OR the literal string "ongoing">
---

# Squad goal

<!--
  Write ONE paragraph that states the OUTCOME, not the ask.

  Goals must be:
    - measurable (a number, a percentage, a count, a boolean signal)
    - time-bounded (a deadline, or the literal "ongoing" for Evergreen mode)
    - outcome-framed (what's TRUE when this is done — not what you'll DO)

  Pattern:  <verb> <deliverable> with <quality bar> by <deadline>

  Examples by mode:

    One-time:
      "Deliver a ranked list of Klaviyo lifecycle fixes with revenue impact
       estimates per fix, within one week."

    Multi-use:
      "Ship a new homepage that converts at >5% with the existing brand
       voice, deployed by end of sprint."

    Evergreen:
      "Every Monday produce a 1-page summary of competitor pricing, product,
       and positioning shifts from the prior week."

  If you find yourself writing "improve X" or "make X better" — stop. That's
  an ask. Reformulate: what's the measurable signal, and by when?
-->

<your outcome paragraph here>

## Definition of done

<!--
  3-5 observable signals that are TRUE when the squad has succeeded.
  Each must be checkable without judgement calls.

  Good signals:
    - "Lighthouse Performance score >= 90 on mobile"
    - "Klaviyo flow revenue attribution increases by >= 15% over baseline"
    - "All 8 audit findings have a documented owner and ETA"

  Bad signals (too vague):
    - "site is fast"
    - "code is clean"
    - "users are happy"
-->

- <signal 1>
- <signal 2>
- <signal 3>

## Out of scope

<!--
  Explicit exclusions that protect the goal from drift mid-execution.
  This is where you record what you DECIDED NOT to do, so a future
  teammate doesn't accidentally re-open settled questions.

  Examples:
    - "iOS native app — web only for v1"
    - "Refactor of the auth module — track separately"
    - "International expansion — US English only"
-->

- <exclusion 1>
- <exclusion 2>
